let skinName = 'katsuLiminal'
let exportName = 'dethz-1.20.1'

let sourceFolder = 'src/'
let exportFolder = 'dist'


// Pls Don't touch me :(
let outputSource = exportFolder + '/' + exportName + '.zip'

module.exports = {
  skinName,
  exportName,
  sourceFolder,
  exportFolder,
  outputSource
}